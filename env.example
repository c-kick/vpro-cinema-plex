# VPRO Cinema Plex Provider - Environment Configuration
# Copy this file to .env and edit as needed

# =============================================================================
# RECOMMENDED: TMDB API Key
# =============================================================================
# Enables automatic alternate title lookup for non-Dutch film titles.
# Many films are indexed in VPRO Cinema under their original title (French,
# German, etc.) rather than the English title. With a TMDB key, the provider
# automatically finds and tries alternate titles.
#
# Get your free API key at: https://www.themoviedb.org/settings/api
#
TMDB_API_KEY=

# =============================================================================
# OPTIONAL: Server Configuration
# =============================================================================

# Port exposed on the host (container always runs on 5100 internally)
# HOST_PORT=5100

# Logging level: DEBUG, INFO, WARNING, ERROR
# LOG_LEVEL=INFO

# =============================================================================
# OPTIONAL: Additional Metadata Fields
# =============================================================================
# By default, only description and content rating (Kijkwijzer) are returned.
# Enable these to also return VPRO images and/or ratings.
#
# WARNING: Enabling these may override data from secondary agents (Plex Movie).
# Only enable if you want VPRO to be the sole source for these fields.

# Return VPRO images (posters/artwork) to Plex
# VPRO_RETURN_IMAGES=false

# Return VPRO appreciation rating (1-10) as Plex rating field
# VPRO_RETURN_RATING=false
